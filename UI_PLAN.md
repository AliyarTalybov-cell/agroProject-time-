# План на завтра: приводим интерфейс в порядок

Договорились 24.09.2026. Цель — чтобы сайт перестал выглядеть «стрёмно»: единые
отступы, шрифты и компоненты на всех страницах, больше воздуха, меньше шума.
Работаем по одному разделу, после каждого — скриншоты «было / стало»
(десктоп и телефон 390px), пользователь смотрит и даёт добро.

## Эталон — shadcn-vue

Визуальный ориентир выбран: **https://www.shadcn-vue.com/** (Vue-порт shadcn/ui).
Нейтральные серые тона, тонкие рамки, мягкие тени, радиус ~8px, аккуратные
поля, таблицы и вкладки, много воздуха.

**Цель пользователя — максимально похоже на эталон.** Решение — компромисс:

- **Простые компоненты** (кнопка, поле, select, таблица, вкладки, карточка,
  бейдж, пагинация, окно, пустое состояние) — свои, в `components/ui/` и
  `ui.css`, но с размерами, отступами, рамками, тенями и состояниями
  (hover / focus / disabled) **один в один из исходников shadcn-vue**.
  Исходники: github.com/unovue/shadcn-vue, каталог
  `apps/v4/registry/new-york-v4/ui/<компонент>/` (MIT). Классы Tailwind
  переводить в CSS по точным значениям, не на глаз. Основной цвет — наш
  зелёный вместо их чёрного, остальное как у них.
- **Сложные компоненты** (выпадающий список с поиском / combobox, выбор даты,
  popover, dropdown-menu, диалог при необходимости) — поведение берём из
  **Reka UI** (`reka-ui`, та же headless-библиотека, на которой построен
  shadcn-vue: клавиатура, позиционирование, доступность), оформление — их же
  стили, переведённые в наш CSS. Tailwind **не** ставим.
- **Боковое меню** — как sidebar shadcn-vue (`ui/sidebar/`): группы с
  подписями, раскрывающиеся подменю, активный пункт мягкой подложкой,
  сворачивание до иконок на десктопе, на телефоне — выезжающая панель.
  Пункты и порядок меню не меняем (правило `ui-navigation-consistency.mdc`),
  значки «?» у «Склады» / «Учёт зерна» **убрать из меню** (решено с
  пользователем); подсказки остаются внутри страниц и форм.
- Установка `reka-ui` — новая зависимость, согласовать с пользователем перед
  `npm install`.

Страницы переводим по одной, старое не ломаем. Полный переход на Tailwind +
shadcn-vue не делаем.

Пользователь сомневается, что всё пройдёт гладко, — поэтому начинать с
малого: сначала токены и один экран, показать «было / стало», и только после
его «ок» идти дальше.

## Главное требование пользователя — ничего не сломать

Слова пользователя: взять с shadcn-vue всё, но применять **очень аккуратно** —
в проекте карта, геометки и т. д.; **ничего нигде не сломать**, делать
супер аккуратно и **правильно**: единые отступы, пагинация, компоненты.

Правила, которые из этого следуют:

- **Никаких глобальных сбросов по тегам.** Не менять стили `button`, `input`,
  `select`, `table`, `a`, `*`, `svg`, `img` глобально — только классы `ui-*` и
  компоненты `components/ui/`. Иначе поедут карта и сторонние виджеты.
- **Карта — зона неприкосновенности.** Яндекс Карты (`components/YandexMap.vue`,
  `lib/yandexGeocode.ts`) используются в `FieldsPage`, `FieldDetailsPage`,
  `LandsPage`, `WeatherPage`. Внутренности карты (`[class*="ymaps"]`,
  балуны, метки, контролы) не стилизовать; меняется только рамка/карточка
  вокруг. После каждого шага проверять: карта грузится, метки видны,
  **постановка геометки кликом работает**, балуны открываются, геокодинг
  адреса находит точку.
- **Логику не трогать.** Меняем разметку и стили; обработчики, `v-model`,
  запросы в базу, роуты и права — как были. Если при замене компонента
  приходится менять привязки — сверить поведение до/после.
- **Смело и глобально, но экономно** (уточнение пользователя: «не надо после
  мелкой правки весь сайт смотреть регрессом — токены сожжёшь, делай
  глобально, смотри одну страницу и поехали»). Меняем токены и компоненты
  сразу для всего сайта, проверяем на одной показательной странице и идём
  дальше. Смотреть только то, что правка реально задевает; карту — только
  если правка могла до неё дотянуться. Коммиты осмысленными порциями, чтобы
  можно было откатить.
- **Один широкий прогон — перед выкладкой** (десктоп + телефон, светлая +
  тёмная): ключевые экраны, карта с геометкой, формы. Не после каждого шага.
- Типы, юнит-тесты, сборка — перед коммитом. Правила
  `.cursor/rules/` (особенно `architecture-and-compatibility.mdc` про карту
  и погоду, `ui-navigation-consistency.mdc`, `pr-safety-checklist.mdc`)
  перечитать перед началом.

## Первый шаг завтра — выбрать цвета

Пользователь хочет взять цвета из shadcn, похожие на наши, чтобы всё было чётко.
Сделать маленькую страницу-сравнение: одна и та же кнопка, вкладка, таблица,
пункт меню в вариантах основного цвета — наш `#2d5a3d` (`global.css`),
green-800, green-900, emerald-800 из палитры shadcn/Tailwind и Emerald из темы
(`theme-emerald.json`, primary `142 86% 28%`) — на серых **Stone** (рекомендую:
тёплый, к агро) и **Zinc**; светлая и тёмная тема. Точные значения брать из
`apps/v4/assets/css/themes.css` и `public/r/styles/new-york-v4/theme-*.json`
в репозитории shadcn-vue. Пользователь выбирает — дальше эти значения идут в
токены `ui.css` / `global.css`.

## Потом спросить у пользователя

1. Какие экраны кажутся самыми стрёмными (назвать или скинуть скриншоты).

От ответа зависит порядок экранов в шаге 4.

## Почему сейчас плохо (что видно по коду)

- Страницы собирались в разное время, у каждой своя вёрстка. Своя пагинация
  примерно на 10 страницах (`fields-pagination` в FieldsPage/LandsPage,
  `task-pagination`, `emp-pagination`, `equipment-pagination`, `news-pagination`,
  `field-history-pagination`, `equipment-task-pagination`), свои таблицы,
  вкладки, фильтры, кнопки.
- Отступы и радиусы гуляют (16 / 24 px, разная высота полей), размеры
  шрифта и заголовков разные; где-то заголовок и в шапке, и на странице.
- Шум: подписи капсом, карточка в карточке, рамки везде, значки «?» даже в
  боковом меню («Склады», «Учёт зерна»).

## Шаги

1. **Основа в `frontend/src/styles/ui.css`** — токены: шкала отступов
   8/12/16/24/32, 3–4 размера текста, один радиус, одна поверхность карточек.
   Проверить, что старые страницы не поехали.
2. **Общие компоненты везде.** Перевести страницы на `UiPagination`,
   `ui-toolbar` / `ui-search` / `ui-filter-select`, `ui-table` + `v-card-table`,
   `ui-tabs`, `ui-empty`. Эталон — складские страницы (`pages/stock/`).
   Если компонента нет — сначала вынести в `components/ui/`, потом использовать
   (правило 4 в CLAUDE.md).
3. **Меньше рамок, больше воздуха.** Убрать вложенные карточки и капс-подписи,
   спокойная типографика, один зелёный акцент только на действиях.
4. **Ключевые экраны вручную:** «Обзор», «Склады», «Поля», «Задачи» — затем
   остальные (Техника, Земли, Сотрудники, Новости, Чат, Профиль, Аналитика).
5. **Лёгкая анимация** появления и наведения — вместо лишних значков.

## Как работать

- Ветка от `main` (например `feature/ui-cleanup`), коммиты на русском.
  Push и деплой — только после явного «да» пользователя.
- Проверка — дев-сервер `localhost:5173` на тестовой копии базы (туннель
  `ssh -N -L 18000:127.0.0.1:18000 root@72.56.248.97`). В скрытой вкладке Chrome
  анимации стоят — перед скриншотом внедрять CSS с `animation-duration:0s`,
  `transition:none`; мобильный вид — страница в iframe шириной 390px.
- Перед отчётом: `npx vue-tsc -b`, `npx vitest run`, `npm run build`,
  скриншоты каждого изменённого экрана в светлой и тёмной теме.
- Экономить лимиты: правки пачками, проверка скриншотами только изменённого.

## Хвосты с прошлого раза

- `user_notifications` отдаёт 503 (и на тестовой, и на боевой), а страница
  уведомлений показывает «Пока уведомлений нет» вместо ошибки — разобраться.
- Копия сайта на Vercel не обновлена после деплоя складского учёта — спросить,
  нужна ли она.

## Полный перенос на компоненты shadcn-vue (25.09, после замечания пользователя)

Пользователь: «ты должен сделать так как я скидывал сайт — там все компоненты
для всего, ты должен все перенять»; «модалки, иконки, чат, все разные механизмы —
ты сделал только верхушку айсберга». Поэтому — настоящие компоненты shadcn-vue
(CLI, `components/ui/shadcn`), Tailwind без глобального preflight, утилиты только
из `components/ui` (см. `styles/shadcn.css`). Ветка `feature/shadcn-full`.

Опись и ход работ:

- [x] Tailwind + CLI shadcn-vue, 35 компонентов, тема = наши цвета (Zinc + #2d5a3d)
- [x] UiButton → Button, UiModal → Dialog, UiConfirmModal → AlertDialog
- [x] UiSelect → Select, UiDatePicker → Popover + Calendar, UiDateTimePicker → + Input
- [ ] Самописные окна (19 файлов): земли ×6, сотрудники ×2, задачи, календарь,
      поля, профиль, техника (просмотр документа), чат, экран оператора,
      «сервер недоступен», «успешно», App.vue
- [ ] `prompt()` ×10 в «Землях» → Dialog
- [ ] Иконки: 268 inline-svg + эмодзи → Lucide (@lucide/vue), размеры как в shadcn
- [ ] Чат: список диалогов, сообщения, поле ввода, окна
- [ ] Уведомления-тосты → Sonner (вместо UiSuccessModal и самописных сообщений)
- [ ] Подсказки «?» (RefFieldHelp) → Tooltip / Popover
- [ ] Поиск сотрудника/исполнителя и другие самописные выпадашки → Combobox
- [ ] Чекбоксы, радио, переключатели и сегменты → Checkbox, RadioGroup, Switch, ToggleGroup
- [ ] Загрузка файлов (11 мест) — единый вид на Button/Empty
- [ ] Индикаторы загрузки → Spinner / Skeleton
- [ ] Пустые состояния → Empty
- [ ] Кнопки и поля страниц → Button / Input / Textarea / Label
- [ ] Таблицы → Table, карточки → Card, бейджи → Badge, вкладки → Tabs, пагинация → Pagination
- [ ] Боковое меню → Sidebar (сворачивание до иконок, на телефоне — Sheet)
- [ ] Страницы вручную: склады, поля, задачи, календарь, техника, земли,
      сотрудники, профиль, новости, чат, аналитика, погода, экран оператора, вход
- [ ] Лёгкие анимации
- [ ] Уведомления: 503 от user_notifications и честная ошибка на странице
- [ ] Широкая проверка: светлая/тёмная, телефон, карта с геометкой
